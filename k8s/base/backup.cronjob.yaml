# 🧩 Backup CronJob — STUB. No working YAML.
# TODO(student): schedule scripts/backup.sh (once you've implemented it) in-cluster.
# REQUIREMENTS / HINTS:
#   - What schedule meets your RPO? (see scripts/restore.sh hints)
#   - Which Secret gives it DB access? Which identity gives it S3 write access
#     WITHOUT long-lived keys?
#   - How do you get alerted when a backup FAILS? (a CronJob that fails silently
#     is worse than none.)
#   - Acceptance: a scheduled run produces a timestamped object in the bucket, and
#     restore.sh can bring the data back — demonstrated end-to-end.
