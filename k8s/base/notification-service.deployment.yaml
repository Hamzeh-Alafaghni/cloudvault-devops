# 🧩 notification-service — Deployment + Service. STUB (your graded work). No working YAML here.
#
# TODO(student): write a Deployment and a Service for "notification-service".
# REQUIREMENTS / HINTS (questions, not answers):
#   - What image? (the one CI pushes to GHCR). How is the tag chosen per environment?
#   - Liveness probe -> GET /healthz ; Readiness probe -> GET /readyz. Why two?
#     What should each probe's failureThreshold / timing be, and what happens to
#     traffic when readiness fails but liveness passes?
#   - Config via ConfigMap, secrets via Secret — NEVER hard-code either in here.
#   - Requests/limits: what does this service actually need? What breaks if you omit them?
#   - Replicas: which services are stateless and safe to scale? (thumbnail-worker
#     is a queue consumer — does adding replicas just work? why/why not?)
#   - This one is reached via the Service by the gateway or the Ingress.
