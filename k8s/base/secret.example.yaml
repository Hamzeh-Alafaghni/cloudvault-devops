# 🧩 Secret — EXAMPLE STUB ONLY. Do NOT commit real secrets.
# TODO(student): define the shape of the secrets the app needs (JWT_SECRET, DB
# passwords, object-store creds if any).
# HINTS:
#   - Why is a base Kubernetes Secret only base64, not encryption? What are your
#     options for real secret management (sealed-secrets, external-secrets, SOPS,
#     a cloud secrets manager)? Pick one and justify it.
#   - How do you keep the REAL secret file out of Git? (it must be git-ignored)
